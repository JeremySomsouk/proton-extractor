[package]
name = "proton-extractor"
version = "0.1.0"
edition = "2021"

[features]
default = ["color"]
color = []

[dependencies]
ical = "0.11"
chrono = "0.4"
clap = { version = "4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
csv = "1"
